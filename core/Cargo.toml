[package]
name = "vrcs-core"
version = "0.1.11"
description = "VRCS local core service (Rust)"
authors = ["VRCS contributors"]
license = "AGPL-3.0-only"
edition = "2021"

[features]
default = []
cuda = ["whisper-rs/cuda"]

[dependencies]
axum = { version = "0.8", features = ["ws"] }
tokio = { version = "1", features = ["full"] }
tower-http = { version = "0.6", features = ["cors"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rusqlite = { version = "0.32", features = ["bundled"] }
reqwest = { version = "0.12", features = ["json", "rustls-tls", "stream", "multipart"], default-features = false }
zip = { version = "2", default-features = false, features = ["deflate"] }
regex = "1"
chrono = { version = "0.4", default-features = false, features = ["clock", "std"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
tracing-appender = "0.2"
ndarray = "0.17"
ort = { version = "=2.0.0-rc.12", features = ["ndarray"] }
rustfft = "6.4.1"
futures-util = "0.3"
base64 = "0.22"
tokio-tungstenite = { version = "0.29", features = ["rustls-tls-native-roots"] }
whisper-rs = "0.16"
sha2 = "0.10"
serde_ignored = "0.1"
uuid = { version = "1", features = ["v4"] }
thiserror = "2"
rosc = "0.11"
mdns-sd = "=0.20.3"

[target.'cfg(windows)'.dependencies]
wasapi = "0.23"
windows = { version = "0.62", features = [
  "Win32_Foundation",
  "Win32_Media_Audio",
  "Win32_Security_Credentials",
  "Win32_System_Diagnostics_ToolHelp",
  "Win32_System_LibraryLoader",
] }

[target.'cfg(target_os = "linux")'.dependencies]
pipewire = { version = "0.10", features = ["v0_3_65"] }
# `libc` 用于按需加载 NVIDIA 驱动（dlopen "libcuda.so.1"），与 Windows 上的 nvcuda.dll 对称。
libc = "0.2"


[dev-dependencies]
tempfile = "3"
hound = "3.5"
